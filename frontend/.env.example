# URL of the backend API (backend/api.py). No trailing slash needed.
# Local development:
VITE_API_URL=http://localhost:8000
# Production: set this in the Vercel project settings to your deployed backend, e.g.
# VITE_API_URL=https://smartdoc-api.onrender.com
